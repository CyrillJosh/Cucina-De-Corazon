@model Cucina_De_Corazon.Models.Product
@{
    ViewData["Title"] = "Add Product";
}

<div class="container d-flex justify-content-center" style="min-height: 100vh;">
    <div class="card shadow-sm p-4 bg-orange overflow-auto mt-3"
         style="max-width: 500px; width: 100%; max-height: 80vh; scrollbar-width: thin; scrollbar-color: #f57c1f #fff;">
        <h3 class="text-center mb-4 fw-bold text-white">Create Product</h3>

        <form asp-action="Create" method="post">
            <div class="mb-3">
                <label class="form-label fw-semibold text-white">Name</label>
                <input asp-for="ProductName" class="form-control rounded-2" placeholder="Product Name" required />
            </div>

            <div class="mb-3">
                <label class="form-label fw-semibold text-white">Description</label>
                <textarea asp-for="ProductDescription" class="form-control rounded" placeholder="Description" rows="3" required></textarea>
            </div>

            <div class="mb-3">
                <label class="form-label fw-semibold text-white">Price</label>
                <input asp-for="ProductPrice" class="form-control rounded-2" />
            </div>

            <div class="mb-3">
                <label class="form-label fw-semibold text-white">Category</label>
                <select asp-for="CategoryId" class="form-select rounded-2" asp-items="ViewBag.Categories" required>
                    <option value="" disabled selected>Select Category</option>
                </select>
            </div>

            <div class="mb-3">
                <label class="form-label fw-semibold text-white">Picture (optional)</label>
                <input asp-for="ProductPicture" class="form-control rounded-2" placeholder="Image filename" />
            </div>

            <div class="d-grid">
                <button type="submit" class="btn bg-white text-orange fw-bold" style="background: #f57c1f;">Save</button>
            </div>
        </form>
    </div>
</div>
